interface:
  display_name: "猫片生产部署"
  short_description: "猫片项目从新电脑配置、代码验证到生产部署的完整流程"
  default_prompt: "Use $maopian-production-deploy to validate my maopian-tools changes and deploy them to production."
policy:
  allow_implicit_invocation: true
